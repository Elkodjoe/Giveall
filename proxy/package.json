{
  "name": "giveall-appreciation-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies the Appreciation Generator to an LLM provider, keeping the API key server-side without needing Firebase Blaze.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.4.5",
    "wrangler": "^4.0.0"
  }
}
